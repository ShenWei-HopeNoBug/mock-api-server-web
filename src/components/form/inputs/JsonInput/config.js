const defaultAttrsMap = {
  codeEditor: {
    theme: 'dracula',
    'line-wrap': false,
  },
  // 格式化按钮
  formatCodeButton: {
    type: 'primary',
    size: 'small',
  }
};

// 代码编辑器默认样式
const defaultCodeEditorStyle = {
  lineHeight: '20px',
};

export {
  defaultAttrsMap,
  defaultCodeEditorStyle,
};
